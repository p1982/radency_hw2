export interface IButton {

}

export interface IInput {
    
}

export interface ISelect {
    
}

export interface INotes {
    id: number,
    name: string,
    created: Date,
    category: string,
    content: string,
    archive: boolean,
    dates: Array<Date>
}